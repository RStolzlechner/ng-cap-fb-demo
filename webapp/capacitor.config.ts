import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.tauerntec.demo',
  appName: 'ng-cap-fb-demo',
  webDir: 'dist/ng-cap-fb-demo/browser',
};

export default config;
